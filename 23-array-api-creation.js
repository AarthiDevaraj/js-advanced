//  Arrays are indexed collections
//  Arrays are dynamic (both in content and size)
//  Arrays are basically objects (reference types)

let numbers = [0, 2, 4, 5, 6, 7, 8, 8]; //  short-cut and idiomatic

let letters = Array('A', 'B', 'C', 100, true, new Date()); // constructor

console.log(numbers);
console.log(letters);
